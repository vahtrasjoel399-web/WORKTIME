{
  "name": "tooaeg",
  "private": true,
  "version": "0.1.0",
  "description": "GPS time tracking for construction crews — Expo mobile app + Next.js employer panel on Supabase",
  "workspaces": [
    "apps/mobile"
  ],
  "scripts": {
    "mobile": "npm run start --workspace apps/mobile",
    "admin": "npm run dev --workspace apps/admin",
    "test": "npm run test --prefix apps/admin && npm run test --workspace apps/mobile && npm run typecheck --prefix apps/admin && npm run typecheck --workspace apps/mobile",
    "build": "npm run build --prefix apps/admin",
    "db:push": "supabase db push",
    "db:reset": "supabase db reset"
  },
  "dependencies": {
    "next": "16.3.3"
  },
  "engines": {
    "node": ">=20"
  }
}
